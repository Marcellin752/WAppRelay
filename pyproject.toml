[project]
name = "wapprelay"
version = "0.1.0"
description = "WAppRelay - relai de messages WhatsApp"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]
[tool.uv]
package = false
